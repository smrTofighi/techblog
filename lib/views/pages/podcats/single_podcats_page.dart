import 'package:flutter/material.dart';

class SinglePodcatsPage extends StatelessWidget {
  const SinglePodcatsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
